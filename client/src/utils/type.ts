export enum CodeType {
  cpp = 'cpp',
  nodejs = 'nodejs',
  go = 'go',
  bash = 'bash',
  shell = 'shell',
}

export enum CodeEnv {
  cpp = 'cpp:11',
  nodejs = 'nodejs:lts',
  go = 'go:latest',
  bash = 'centos:7',
  shell = 'centos:7',
}

export enum FileSuffix {
  cpp = 'cpp',
  nodejs = 'js',
  go = 'go',
  bash = 'sh',
  shell = 'sh',
}
